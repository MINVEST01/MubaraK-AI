# MubaraK-AI
Платформа для объединения всех Мусульман
# MubaraK-AI

Добро пожаловать в **MubaraK-AI**! Это бэкенд-сервис, построенный на FastAPI, который представляет собой платформу для духовного развития. Он позволяет пользователям анализировать свои духовные запросы, отслеживать прогресс в виде "Барака Поинтов" и управлять пожертвованиями.

Проект следует современным практикам разработки, включая контейнеризацию с помощью Docker, архитектуру на основе сервисов и полноценный стек для мониторинга и трассировки.

## ✨ Особенности

*   **Современный фреймворк**: Быстрый и асинхронный API на [FastAPI](https://fastapi.tiangolo.com/).
*   **База данных**: Надежная реляционная база данных PostgreSQL с асинхронным доступом через `asyncpg`.
*   **ORM и Миграции**: Использование SQLAlchemy для взаимодействия с базой данных и Alembic для управления миграциями схемы БД.
*   **Кэширование**: Интеграция с Redis для кэширования и других задач.
*   **Контейнеризация**: Полная настройка для разработки и развертывания с помощью Docker и Docker Compose.
*   **Наблюдаемость (Observability)**:
    *   **Трассировка**: Интеграция с Jaeger через OpenTelemetry для отслеживания запросов.
    *   **Метрики**: Сбор метрик с помощью Prometheus.
    *   **Визуализация**: Дашборды в Grafana для визуализации метрик.
*   **Автоматические бэкапы**: Ежедневное резервное копирование базы данных (с хранением за последние 7 дней) и отправкой уведомлений о сбоях в Telegram.
*   **Структурированная кодовая база**: Четкое разделение логики по модулям (API, ядро, база данных).

## 🚀 Начало работы

Для запуска проекта вам понадобятся Docker и Docker Compose.

### 1. Клонирование репозитория

```bash
git clone <your-repository-url>
cd MubaraK-AI
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корневой директории проекта, скопировав содержимое из `.env.example` (рекомендуется создать этот файл).

```bash
cp .env.example .env
```

Заполните `.env` необходимыми значениями. Минимально необходимые переменные:

```dotenv
# Настройки PostgreSQL
POSTGRES_USER=your_user
POSTGRES_PASSWORD=your_strong_password
POSTGRES_DB=mubarak_db

# URL для подключения к БД. Используется приложением и Alembic.
# Формат: postgresql+asyncpg://<user>:<password>@<host>:<port>/<db_name>
# Для Docker-окружения хост - это имя сервиса, т.е. 'db'.
DATABASE_URL=postgresql+asyncpg://your_user:your_strong_password@db:5432/mubarak_db

# Настройки для уведомлений о сбоях бэкапов в Telegram
TELEGRAM_TOKEN=your_telegram_bot_token
TELEGRAM_CHAT_ID=your_telegram_chat_id
```

### 3. Запуск проекта

Проект имеет раздельную конфигурацию для разработки и production.

#### Запуск для разработки (Development)

В этом режиме используется `docker-compose.override.yml` для включения **live-reload** (автоматическая перезагрузка при изменении кода) и проброса портов баз данных для удобной отладки.

Для запуска выполните:

```bash
docker-compose up -d --build
```

Эта команда запустит:
*   `web`: ваше FastAPI-приложение.
*   `db`: базу данных PostgreSQL.
*   `redis`: сервер Redis.
*   `jaeger`: для сбора трейсов.
*   `prometheus`: для сбора метрик.
*   `grafana`: для визуализации метрик.
*   `backup`: для резервного копирования БД.

### 4. Применение миграций базы данных

После первого запуска контейнеров необходимо применить миграции для создания таблиц в базе данных.

```bash
docker-compose exec web alembic upgrade head
```

## 🛠️ Использование

*   **API документация**: Документация OpenAPI доступна по адресу http://localhost:8000/docs.
*   **Jaeger UI**: Интерфейс для просмотра трейсов доступен по адресу http://localhost:16686.
*   **Prometheus UI**: Метрики можно посмотреть по адресу http://localhost:9090.
*   **Grafana UI**: Дашборды для визуализации метрик доступны по адресу http://localhost:3000 (логин/пароль: `admin`/`admin`).

## 🧪 Запуск тестов

Для запуска тестов выполните команду:

```bash
docker-compose exec web pytest
```

## 📁 Структура проекта

```
MubaraK-AI/
├── .env                    # Переменные окружения
├── docker-compose.yml      # Определение и запуск сервисов
├── Dockerfile              # Инструкции для сборки Docker-образа
├── requirements.txt        # Python-зависимости
├── README.md               # Эта документация
├── src/                    # Исходный код приложения
│   ├── api/                # Модуль API (эндпоинты, схемы)
│   ├── core/               # Ядро приложения (бизнес-логика, конфигурация)
│   ├── db/                 # Модуль для работы с БД (модели, сессии)
│   ├── models/             # Доменные модели (Pydantic)
│   └── main.py             # Точка входа в приложение FastAPI
└── tests/                  # Папка с тестами
```

Этот файл закладывает прочную основу для документации вашего проекта. Вы можете дополнить его более подробным описанием бизнес-логики и конкретных эндпоинтов.
Вот разбивка основных модулей вашего приложения, находящихся в папке src:

1. Модуль src/api — Слой представления (Presentation Layer)
Это "входные ворота" вашего приложения. Он отвечает исключительно за обработку HTTP-запросов и ответов.

api/v1/endpoints/: Здесь живут ваши эндпоинты (роуты). Каждый файл (например, users.py, login.py) группирует связанные эндпоинты (/users/, /users/me). Их задача — принять запрос, вызвать необходимую бизнес-логику и вернуть ответ клиенту.
api/v1/schemas/: Содержит Pydantic-схемы. Они используются для валидации данных, приходящих от клиента (например, UserCreate), и для форматирования данных, отправляемых клиенту (User). Это обеспечивает строгую типизацию и надежность вашего API.
api/deps.py: Файл для "зависимостей" (Dependencies). Здесь вы определяете переиспользуемые блоки кода, которые могут быть внедрены в эндпоинты. Отличные примеры — get_current_user и get_current_admin_user, которые инкапсулируют логику аутентификации и проверки прав.
api/v1/router.py: Файл-агрегатор, который собирает все роутеры из endpoints в один главный api_router. Это помогает поддерживать порядок и легко управлять версиями API.
2. Модуль src/core — Ядро приложения (Core/Business Logic Layer)
Это "мозг" вашего сервиса. Здесь находится основная бизнес-логика, конфигурация и сквозная функциональность, не привязанная к конкретным эндпоинтам.

core/config.py: Управляет всеми настройками приложения. С помощью pydantic-settings он считывает переменные из .env файла, валидирует их и предоставляет в виде удобного объекта settings.
core/security.py: Отвечает за все, что связано с безопасностью: хеширование и проверка паролей, создание и валидация JWT-токенов.
core/middlewares.py: Содержит ваши middleware — функции, которые обрабатывают каждый запрос и ответ. Примеры: добавление заголовков безопасности (SecurityHeadersMiddleware) и ограничение частоты запросов (limiter).
core/observability.py: Настраивает систему мониторинга и трассировки (Prometheus и Jaeger), делая ваше приложение "прозрачным" для наблюдения.
core/notifications.py: Здесь инкапсулирована логика отправки уведомлений, например, ежедневная рассылка аятов, которая запускается планировщиком из main.py.
3. Модуль src/crud — Слой доступа к данным (Data Access Layer)
Этот слой выступает посредником между бизнес-логикой (эндпоинтами) и базой данных. Его главная задача — инкапсулировать все SQL-запросы.

Файлы, такие как crud/user.py, содержат функции (get_user, create_user и т.д.), которые выполняют конкретные операции с базой данных (SELECT, INSERT, UPDATE).
Преимущество: Ваши эндпоинты не содержат сложного SQL-кода. Они просто вызывают функцию, например, crud_user.create_user(...). Это делает код чище, упрощает тестирование и позволяет легко заменить способ хранения данных в будущем.
4. Модуль src/db — Слой базы данных (Database Layer)
Здесь находится все, что непосредственно связано с настройкой и определением структуры базы данных.

db/models.py: Содержит SQLAlchemy-модели. Каждый класс здесь представляет таблицу в вашей базе данных.
db/session.py: Управляет подключением к базе данных. Создает engine (движок) и предоставляет зависимость get_db для получения сессии в эндпоинтах.
db/base.py: Обычно здесь определяется базовый класс для всех SQLAlchemy-моделей (Base), от которого они наследуются.
Папка src/db/versions/ (создается Alembic): Здесь Alembic хранит файлы миграций, которые описывают изменения в схеме вашей БД.
5. Модуль src/models — Слой доменных моделей (Domain Layer)
Этот слой определяет основные бизнес-сущности вашего приложения.

models/enums.py: Содержит перечисления (Enum), такие как UserRole. Использование Enum вместо обычных строк ("admin", "user") делает код более надежным и защищает от опечаток.
Как все это работает вместе (Пример запроса)
Клиент отправляет запрос на POST /api/v1/users/ с JSON-данными.
FastAPI направляет запрос в эндпоинт create_user в users.py.
Pydantic-схема UserCreate автоматически валидирует входящие данные.
Эндпоинт вызывает функцию crud_user.get_user_by_email(...), чтобы проверить, не занят ли email.
Затем он вызывает crud_user.create_user(...).
Функция в crud создает экземпляр SQLAlchemy-модели User из db/models.py и сохраняет его в базу данных через сессию, полученную из get_db.
Результат возвращается в эндпоинт, который, в свою очередь, преобразует его в схему User из api/schemas/user.py и отправляет клиенту в виде JSON-ответа.
